name: Nightly

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

jobs:
  e2e:
    name: E2E (Patrol)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO Patrol e2e on Android emulator matrix"

  load:
    name: Load and latency k6
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO k6 against staging assert feed read p95 under 150ms"
