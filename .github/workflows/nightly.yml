name: Nightly

on:
  schedule:
    - cron: '0 3 * * *'   # 03:00 UTC daily
  workflow_dispatch:

jobs:
  e2e:
    name: E2E (Patrol)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: Patrol e2e on Android emulator matrix (onboarding → seed → feed → save-to-brain)"

  load:
    name: Load / latency (k6)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: k6 against staging; assert feed read p95 < 150ms, action commit p95 < 200ms"
